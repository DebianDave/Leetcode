public class Solution {
    public void ReverseString(char[] s) 
    {
        var length = s.Length;
        for(int i = 0; i < length/2; i++)
        {
            var temp = s[i];
            s[i] = s[length - i - 1];
            s[length - i - 1] = temp;
        }
    }
}
